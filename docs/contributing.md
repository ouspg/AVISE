# TODO: Write documentation on how to contribute code to the repository

- Before making a pull request, run unit-tests in the root directory with the command `pytest`. All tests should pass.
> [!NOTE]
> The unit tests rely on `avise/configs/model/model.json` and `avise/configs/SET` configuration files by default.